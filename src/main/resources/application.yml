server :
  port : 8080
  servlet :
    context-path : '/'

spring :
  datasource :
    driver-class-name : com.mysql.cj.jdbc.Driver
    url :               'jdbc:mysql://localhost:3306/third'
    username :          root
    password :          root

  security :
    oauth2 :
      client :
        registration :
          # Google OAuth2 Config
          google :
            client-id :     61528740066-savt6auv1dqdqkcp6pdk7trt331g82rq.apps.googleusercontent.com
            client-secret : hkPHfzaZes8H1erMhOtZYtRr
            scope :
              - profile
              - email
          # Naver Oauth2 Config
          naver :
            client-id :                nrcctKic2og07bgqX85X
            client-secret :            MLn8ZgNyJA
            redirect_uri :             '{baseUrl}/{action}/oauth2/code/{registrationId}'
            authorization-grant-type : authorization_code
            scope :                    name, email.profile_image
            client-name :              Naver

        provider :
          naver :
            authorization-uri :   https://nid.naver.com/oauth2.0/authorize
            token-uri :           https://nid.naver.com/oauth2.0/token
            user-info-uri :       https://openapi.naver.com/v1/nid/me
            user-name-attribute : response

  jpa :
    hibernate :
      ddl-auto : create-drop

  devtools :
    livereload :
      enabled : true
      port :    35279

  redis :
    host : localhost
    port : 6379

  session :
    jdbc :
      initialize-schema : always
      table-name :        SPRING_SESSION
    store-type : jdbc